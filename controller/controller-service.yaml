# Controller Service 
apiVersion: v1
kind: Service
metadata:
  name: cerebro-controller-service
  namespace: {{ .Release.Namespace }}
  labels:
    type: cerebro-controller-svc
    serviceApp: dask
spec:
  selector:
    app: cerebro-controller
  ports:
  - protocol: "TCP"
    port: {{ .Values.controller.daskPort }}
    targetPort: {{ .Values.controller.daskPort }}
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: cerebro-controller-port-forward-jupyter
  namespace: {{ .Release.Namespace }}
  labels:
    type: cerebro-controller-svc
    serviceApp: jupyter
spec:
  selector:
    app: cerebro-controller
  ports:
    - port: {{ .Values.controller.jupyterPort }}
      targetPort: {{ .Values.controller.jupyterPort }}
  type: NodePort
---
apiVersion: v1
kind: Service
metadata:
  name: cerebro-controller-port-forward-tnsrbrd
  namespace: {{ .Release.Namespace }}
  labels:
    type: cerebro-controller-svc
    serviceApp: tensorboard
spec:
  selector:
    app: cerebro-controller
  ports:
    - port: {{ .Values.controller.tensorboardPort }}
      targetPort: {{ .Values.controller.tensorboardPort }}
  type: NodePort
---
apiVersion: v1
kind: Service
metadata:
  name: cerebro-controller-port-forward-dask
  namespace: {{ .Release.Namespace }}
  labels:
    type: cerebro-controller-svc
    serviceApp: dask
spec:
  selector:
    app: cerebro-controller
  ports:
    - port: {{ .Values.controller.daskDashboardPort }}
      targetPort: {{ .Values.controller.daskDashboardPort }}
  type: NodePort