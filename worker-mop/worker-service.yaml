# Worker Service 
apiVersion: v1
kind: Service
metadata:
  name: cerebro-worker-mop-service-{{ .Values.workerID }}
  namespace: {{ .Release.Namespace }}
  labels:
    type: cerebro-worker-mop
spec:
  selector:
    app: cerebro-worker-mop-{{ .Values.workerID }}
  ports:
  - name: "rpc-port"
    protocol: "TCP"
    port: {{ .Values.workerMOP.rpcport }}
    targetPort: {{ .Values.workerMOP.rpcport }}
  type: ClusterIP